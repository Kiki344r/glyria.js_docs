---
title: glyria dev
description: Start your bot in development mode with hot reload.
navigation:
  icon: i-lucide-zap
---

## Usage

```bash
npm run dev
# or
npx glyria dev
```

## What it does

`glyria dev` starts your bot with hot reload — no manual restarts needed while you develop.

## Hot reload behavior

glyria.js watches your files and reacts differently depending on what changed:

| Changed file            | Behavior                                            |
| ----------------------- | --------------------------------------------------- |
| `src/commands/**`       | Hot reloads commands only — no restart              |
| `src/events/**`         | Hot reloads events only — no restart                |
| `src/components/**`     | Hot reloads components only — no restart            |
| `src/modules/**`        | Hot-swaps modules — no restart                      |
| `glyria.config.ts`      | Hot reloads config + regenerates types — no restart |
| `autoImportDirs` files  | Regenerates types instantly — no restart            |
| `src/index.ts`          | Regenerates types instantly — no restart            |
| `dev.restartPaths` files | Full bot restart                                   |
| Any other `src/**` file | Ignored                                             |

The watcher reacts to `.ts` files being changed, created or deleted. This means editing a command, event or component is near-instant — the bot doesn't disconnect from Discord.

## Module hot-swap

Changes under `src/modules/**` hot-swap the module system: manifests are re-imported, `setup()` runs again and hooks are re-registered — without a restart.

If the changed file lives in a module's nested `commands/`, `events/` or `components/` folder, glyria.js also reloads the matching manager, so nested handlers are re-routed exactly like their top-level `src/` counterparts.

## Interaction recording

While `glyria dev` is running, every command execution is automatically recorded to `.glyria/interactions/` — the payload, options, user, replies and any error.

You can then re-run any of them against your current code with [`glyria replay`](/cli/replay). No config needed in dev; use `recording: true` in `glyria.config.ts` to also record in production.

## Type generation

Whenever a file in your auto-import folders changes, glyria.js automatically regenerates `.glyria/imports.d.ts` so your IDE stays in sync without running `glyria generate` manually.

This also runs when `glyria.config.ts` changes — in case you updated `autoImportDirs`.

## Environment

`glyria dev` sets the following environment variables automatically:

```bash
NODE_ENV=development
GLYRIA_DEV=true
```

`GLYRIA_DEV=true` switches the command loader to use `src/` and `.ts` files directly instead of `dist/` — no build step needed in dev.

## Anti double-trigger

glyria.js uses [chokidar](https://github.com/paulmillr/chokidar) with a 200ms stability threshold per file — the watcher waits for the file to stop changing before reacting. This prevents double triggers caused by editors, ESLint, or Prettier writing the file multiple times on save.

## TypeScript

Your TypeScript files are executed directly via `tsx` — no compilation needed during development. Just save and glyria.js handles the rest.
